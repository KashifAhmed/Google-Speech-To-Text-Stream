{
  "name": "stt-server",
  "version": "1.0.0",
  "description": "Real-time Speech-to-Text server with Google Cloud STT",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for Node.js server'",
    "postinstall": "echo 'Dependencies installed successfully'",
    "health": "node -e \"console.log('Server health check passed')\"",
    "test": "echo 'No tests specified yet' && exit 0"
  },
  "keywords": [
    "speech-to-text",
    "websocket",
    "google-cloud",
    "real-time",
    "audio"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.14.2",
    "@google-cloud/speech": "^6.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}